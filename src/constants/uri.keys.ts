export enum streamUris {
  stats = '/stream/stats',
  add = '/stream/add',
  add_magnet = '/stream/add/:magnet',
  stream_magnet_fileName = '/stream/:magnet/:fileName'
}

export enum contentUris {
  boostrap = '/content/'
}